{% extends 'base.html.twig' %}

{% block title %}Mes adresses - Delta Ecommerce{% endblock %}

{% block content %}
    <a href="{{ path('account_address_add') }}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
    <h1>Mes adresses</h1>
    <p>C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.</p>
    <a href="{{ path('account') }}">Retour</a>
    <hr>
    {% if app.user.addresses|length == 0 %}
        <p class="text-center">Vous n'avez pas encore ajouté d'adresse dans votre compte client. Pour en ajouter une, veuillez <a href="">cliquer ici.</a></p>
    {% else %}
        Afficher les addresses
    {% endif %}
{% endblock %}
