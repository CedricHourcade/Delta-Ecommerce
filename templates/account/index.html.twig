{% extends 'base.html.twig' %}

{% block title %}Mon compte - Delta Ecommerce{% endblock %}

{% block content %}
    <h1>Mon compte</h1>
    {% if is_granted('ROLE_ADMIN') %}
        <small><a href="/admin">- Vous êtes un administrateur, accéder au backoffice</a></small><br/>
    {% endif %}

    <p>Bienvenue {{ app.user.firstname }} dans votre compte.<br/>
    C'est dans cet espace que vous allez pouvoir gérer toutes vos informations personnelles.</p>
    <hr>

    <div class="row">
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/password.png') }}" class="d-block m-auto" alt="Modifier mon mot de passe">
                <a href="{{ path('account_password') }}">Modifier mon mot de passe</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/contact-book.png') }}" class="d-block m-auto" alt="Gérer mes adresses">
                <a href="{{ path('account_address') }}">Gérer mes adresses</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/checklist.png') }}" class="d-block m-auto" alt="Mes commandes">
                <a href="{{ path('account_order') }}">Mes commandes</a>
            </div>
        </div>
    </div>
{% endblock %}
